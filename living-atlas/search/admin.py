from django.contrib import admin

# Register your models here.

# viking: in general, commented-out code can be removed since it will be
# preserved in source control history. If you need to leave it in, a short
# comment explaining why is always helpful.
# from .models import FrolexEntry

# class FrolexEntryAdmin(admin.ModelAdmin):
#     list_display = ["lemma"]

# admin.site.register(FrolexEntry, FrolexEntryAdmin)
